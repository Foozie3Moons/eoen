include ../layout

.ui.middle.aligned.center.aligned.grid
  .column
    h2.ui.teal.image.header
      img.image(src='assets/images/logo.png')
      .content
        | Log-in to your account
    form.ui.large.form(action='/auth/login', method='POST')
      .ui.stacked.segment
        .field
          .ui.left.icon.input
            i.user.icon
            input#authEmail(type='text', name='email', placeholder='E-mail address')
        .field
          .ui.left.icon.input
            i.lock.icon
            input#authPassword(type='password', name='password', placeholder='Password')
        .ui.fluid.large.teal.submit.button Login
      .ui.error.message
    .ui.message
      | New to us? 
      a(href='#') Sign Up
